# 2026-02-21

## Session Summary

### Tooling Status Overhaul
- Created `jambot/TOOLING-STATUS.md` — single source of truth for tool health
- Referenced it in `jambot/CLAUDE.md` under Workflow
- Final status after testing:
  - ✅ Working: JB01, JT90, JT10, JT30, Delay, Filter
  - ⚠️ Needs tuning: JB202 (triangle renders as sine), Analyze tools
  - 🧪 Untested: JP9000, Sampler, Sidechain compression, EQ
  - ❌ Broken: Reverb (send bus never mixed into render output)
  - 🪦 Deprecated: JB200
- **UPDATE**: Sidechain compression tested and WORKING — moved to ✅

### Hannes Bieger Artist Profile
- Added to `library.json` (2nd artist after Jeff Mills)
- Added Bieger-inspired test pack to CLAUDE.md

### Reverb Bug Root Cause
- `create_send` / `route_to_send` only exist in old bundled `dist/jambot.js`
- New routing system uses `add_send` / `route` (different tool names)
- But even with new tools, `render.js` has NO send/return bus mixing stage
- Dry vs wet renders are bit-identical (confirmed via SHA hash)
- Fix needed: add send processing stage to render.js

### JB202 Waveform Bug
- Set osc1 to triangle, but oscilloscope shows sine wave
- Waveform detection confirms: 57% sine confidence
- Bug in headless waveform selection path

### Tracks Made
- `jt90-kick-ch-delay.wav` — kick + hats + ping-pong delay
- `jb01-kick-ch-reverb.wav` — reverb test (reverb broken, just dry hats)
- `jb202-pure-a.wav` — pure A3 note for analyzer testing
- `jt10-test/loop/full` — JT10 lead synth Cm7 arp (sounds great!)
- `jt30-acid.wav` — 303-style acid bass with slides/accents
- `sc-dry.wav` / `sc-wet.wav` — sidechain A/B test (working!)
- `sc-delay.wav` — JT10 + kick + sidechain + delay
- **`jt10-arranged.wav`** — FULL 48-bar arrangement (~104s):
  - 6 sections: intro→build→drop→peak→bridge→return
  - JT10 arp (Cm7 + Fm7 bridge) + JT90 drums
  - Sidechain compression + ping-pong delay
  - Rendered via manual section concatenation (save_pattern doesn't support JT10)

### Key Findings
- SCP to iMac keeps timing out on large files — use `cp` instead (same machine)
- Discord file limit: 16MB (WAV arrangements exceed this)
- `save_pattern` doesn't support JT10/JT30 yet — had to render sections separately and concatenate
- Filter effect works as insert on instrument output, automation on cutoff confirmed working
- Sidechain is pattern-based (reads kick steps, deterministic envelope) — works well
