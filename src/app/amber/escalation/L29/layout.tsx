import type { Viewport } from 'next'

export const viewport: Viewport = {
  themeColor: '#FFF8E7',
}

export default function L29Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
