export const meta = {
  name: 'jambot-native-full-app',
  description: 'Complete the Jambot native app: Seq + Panels tabs, sharing/catalog/bounce/about, render cache + lock-screen audio, branding/Catalyst; then integrate, design-review and TestFlight',
  phases: [
    { title: 'Build', detail: 'five builders on disjoint files (Fable: seq, audio · Sonnet: panels, tracks, brand)' },
    { title: 'Integrate', detail: 'wire into Studio/Controls, verify in simulator + Catalyst + device' },
    { title: 'Review', detail: 'design pass against the web app' },
    { title: 'TestFlight', detail: 'archive, export, upload' },
  ],
}

const HILMA = '/Users/bartdecrem/Documents/coding2025/hilma'
const APP = `${HILMA}/apps/jamnative`

const SHARED = `
PROJECT: Jambot native (SwiftUI, iOS + Mac Catalyst) at ${APP}. Read first: ${APP}/DESIGN.md (both parts — the test build and "Full app"), ${APP}/PROGRESS.md (stages 5–12; you update yours), ${APP}/CLAUDE.md (build/run/install commands). The test build already works end to end (engine host in a hidden WKWebView, AudioPlayer, Library/Studio/Faders, autosave, agent chat) — extend it, don't rewrite it. The web app you are matching is in ${HILMA}/src/app/jam (design tokens in jam.css; Bart calls the look a "desk instrument": putty panel #e3e4dc, ink keys, cobalt fader caps, 909-orange #ff5a1f LEDs, condensed uppercase labels, mono readouts). Screenshots of the web app for reference can be taken headlessly with ${HILMA}/scripts/jam/pw-harness.mjs (jamtest account) — the dev server may be down; production https://jambot.to works with JAM_URL=https://jambot.to.
RULES
- Build check after every meaningful step: \`cd ${APP} && xcodegen generate && xcodebuild -project Jambot.xcodeproj -scheme Jambot -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3\` → ** BUILD SUCCEEDED **. Catalyst: \`-destination 'platform=macOS,variant=Mac Catalyst'\`. Never open the Xcode GUI.
- NO SCREEN CONTROL: never call computer-use, claude-in-chrome, request_access or anything that drives the Mac's screen — nobody can approve it and a previous agent hung for an hour. The simulator is driven only with xcrun simctl + the app's DEBUG launch arguments (-autoLogin jamtest jamtest1, -openTrack "<title>", -openControls, -studioScript "<steps>"; see UI/StudioScript.swift — extend its vocabulary for your feature, e.g. tab:seq, seq:tap:kick:2, panels:open:jb202, bounce:wav, shot:<name>). Screenshots go under ${APP}/.shots/ (gitignored) and you LOOK at them (Read the png) before calling a screen done.
- FILE OWNERSHIP is strict — other agents are editing the same app right now. Touch only the files listed for you; if you need a change elsewhere, write it under "integration requests" in your report and, where possible, ship your feature as self-contained views/services with a clear Swift API the integrator can call. Do not edit StudioModel.swift, StudioView.swift, ControlsSheetView.swift, JambotApp.swift, EngineFactory.swift or project.yml unless they are in your list (new files need no project.yml change — XcodeGen globs the Jambot folder; run xcodegen generate).
- Backend is production. Only the jamtest / jamtest1 account from the simulator. Never sign in as bart.
- Never name a bundle subfolder "Resources". Keep ENABLE_DEBUG_DYLIB: NO. No third-party packages.
- Do not commit (the integrator commits and pushes). Update PROGRESS.md: check your box and add ONE dated log line (what you built, what you verified with which commands, integration requests). Edit only your own lines.
- Report back concisely: files, public Swift API of what you built (signatures), what you verified (with screenshot paths), integration requests.
`

const BUILDERS = [
  { key: 'seq', model: undefined, effort: 'high', prompt: `${SHARED}
STAGE 5 — SEQ TAB + BRIDGE ADDITIONS (you). Files you own: ${APP}/Jambot/Engine/engine-bridge.js, Engine/EngineHost.swift, Engine/EngineAPI.swift (additive: new methods + types, keep MockEngine conforming), Engine/EngineSmoke.swift (add a -engineSmokeSeq run), and NEW ${APP}/Jambot/UI/Seq/*.swift.
1. Bridge: implement \`hits\`, \`seq\` and \`pattern\` exactly as DESIGN.md "Bridge additions" specifies, porting ${HILMA}/src/app/jam/seq/model.ts (canonical voice lists, sharps-only notes C0–C7, cycleDrumStep, toggle/set helpers, resize/clear, normalise voices, playingSection, hitsAt) into engine-bridge.js. Song-mode edits write session.patterns[inst][name].pattern and mirror into the live node when currentPattern[inst] === name; loop mode edits the live node; resizing in loop mode raises session.bars like the web. Every call answers once; errors → { ok:false, error }.
2. Swift: EngineAPI gets \`func hits(step: Int, scope: RenderScope) async throws -> [String: [String]]\`, \`func seq(op: String, inst: String, section: Int?, args: JSONValue) async throws -> SeqResult\` (SeqResult { desc: SessionDescription, pattern: SeqPattern }), \`func pattern(inst: String, section: Int?) async throws -> SeqPattern\` where SeqPattern is an enum { drums([String: [DrumStep]]), mono([MonoStep]) } with Codable DrumStep { velocity, accent } / MonoStep { note, gate, accent, slide } plus name/length/kind. EngineHost implements them; MockEngine returns canned patterns.
3. UI/Seq/SeqView.swift (+ helpers): the native sequencer per DESIGN.md "Seq" row, matching ${HILMA}/src/app/jam/seq/Sequencer.tsx + seq.css pixel-for-pixel in spirit (instrument Menu styled as a rubber key with ▾, section pills 58×46 with bar counts and a corner LED that is lit for the playing section and pulses on beats, LOOP SECTION LED key, overview strip with the window outline + playhead ring, ‹ BAR 1 · STEPS 1–8 › nav, swipe paging, drum grid with 52pt label column and 34×38 pads (orange hit, ink ring + dot for accent, beat columns shaded, muted labels for silent voices), mono note row with pitch bar + STEP editor card, LENGTH 1·2·4, two-tap CLEAR with 3 s disarm; 16 steps per page when horizontalSizeClass is regular). Inputs: engine (EngineAPI), desc, playStep16, playScope, bindings for the picked instrument/section held by the caller, callbacks onEdited(noteKey: String, noteText: String) and onScope(RenderScope). Own a small SeqModel that loads the pattern via engine.pattern and applies edits via engine.seq (optimistic local update, then replace with the returned pattern). Notes coalesce per inst+pattern like the web ("sequencer edited jt90 pattern B (steps counted from 1): kick step 2 on, …", last 8 then "…").
4. Verify: -engineSmokeSeq in the simulator: pattern(jt90, section 0) → cycleDrum kick step 1 → hit, again → accent, again → off; toggleGate on jb202 step 2 + setNote A#1; resize 2 bars keeps steps; clear; hits at a kick step contains jt90; section-2 edits land in saved pattern B not A (compare via serialize). Paste the log. Then a throwaway harness view (-seqPreview launch arg that shows SeqView with the real engine on the jamtest "SEQ TEST techno copy" track) → screenshots of drums page 1, accent state, mono editor, 2-bar length, wide layout on iPad simulator if available (or Catalyst). Look at them.
5. PROGRESS.md box 5. Report the Swift API + integration requests (the integrator adds the Seq tab to ControlsSheetView and wires onEdited → re-render/autosave/notes and onScope → StudioModel.renderScope).` },
  { key: 'panels', model: 'sonnet', effort: 'high', prompt: `${SHARED}
STAGE 6 — PANELS TAB (you). Files you own: NEW ${APP}/Jambot/UI/Panels/*.swift only.
Build the native Panels per DESIGN.md "Panels" row, matching ${HILMA}/src/app/jam/alt/panels.tsx (markup/structure), alt/Knob.tsx (drag behaviour: 150pt vertical drag = full range, log scale for Hz controls per ControlsMath, double-tap resets to descriptor default, floating orange readout while dragging), alt/panels-mobile.css (accordion header 52pt with LED + name + summary + chevron + M/S keys; narrow layout rules: 4 knobs per row centred, JB202 OSC1|OSC2 side by side then FILTER/ENVs/OUTPUT, JT-10 sections with 44×120 vertical sliders and the LFO row, JT-90/JB01 2-column voice cards with a voice LED, effects panel with choice rows + knobs, mix knob large) and the palettes in alt/panels-mobile.css (--ph-bg/--ph-accent/--ph-glow/--ph-text/--ph-dim/--ph-rule per data-skin) and alt/skins.css (knob face/pointer colours per synth). Public API: \`PanelsView(desc: SessionDescription, hits: [String: [String]], onParam: (path, value, label) -> Void, onMix: (id, what, on) -> Void)\` plus \`KnobControl\`, \`ChoiceRow\`, \`VSliderControl\` reusable. Use the existing Control/ControlGroup/ParamEntry/ParamDescriptor types and ControlsMath (read UI/ControlsMath.swift and Engine/EngineAPI.swift first). Accordion: one open (remembered in UserDefaults jam.panelsOpen), first active instrument by default; header LED and JT-90/JB01 voice LEDs flash when \`hits[inst]\` contains the voice (0.03 s on, 0.18 s ease-out), header dims when muted or solo-silenced (desc.tracks / anySolo). M/S keys sit beside the header button, never inside it.
Verify: a -panelsPreview launch arg (add it ONLY inside your own files by exposing \`PanelsPreviewHost\` and asking the integrator to route it; until then use SwiftUI previews compiled by the build and a temporary harness you remove) — the reliable path: build succeeds + screenshots via a tiny local harness file UI/Panels/PanelsPreview.swift that JambotApp can present; document the launch-arg request. Screenshots: collapsed accordion; JT-90, JB202, JT-10 open; an effect open; a knob mid-drag with the floating readout; iPad/Catalyst width. Look at them and fix spacing until a designer would sign off.
PROGRESS.md box 6. Report API + integration requests.` },
  { key: 'tracks', model: 'sonnet', effort: 'medium', prompt: `${SHARED}
STAGE 7 — TRACKS, SHARING, BOUNCE, ABOUT (you). Files you own: ${APP}/Jambot/JamAPI.swift and Models.swift (additive), UI/LibraryView.swift, UI/LoginView.swift, NEW UI/CatalogView.swift, UI/PublicPlayerView.swift, UI/AboutView.swift, UI/BounceSheet.swift, UI/StudioHeaderActions.swift (rename / Publish / Unpublish / Share controls as a self-contained view taking a small \`SharingState\` ObservableObject you define + callbacks), NEW Services/Exporter.swift, Services/ShareSheet.swift.
- JamAPI: duplicate (POST /api/jam/tracks/:id/duplicate), delete, publish (POST /api/jam/tracks/:id/publish) / unpublish (DELETE same), catalog (GET /api/jam/public), publicTrack (GET /api/jam/public/:slug), remix (POST /api/jam/public/:slug/remix), signup. Public URL: https://jambot.to/t/<slug>. Mirror ${HILMA}/src/app/jam/api.ts shapes.
- Library: "…" menu per track (Duplicate, Delete with confirmation), tags public/remix, pull to refresh, About key in the header (gear or "About"), Catalog section "FROM EVERYONE" under the list (PublicTrackMeta rows with username; tap → PublicPlayerView: title, by <user>, LED strip, Play/Stop rendering the public session through the engine (it needs an EngineAPI — accept one as a parameter; the integrator passes the shared EngineFactory.shared) and Remix → api.remix → open in library). Login screen shows the catalog below the form for signed-out users (read-only list; tapping asks to sign in).
- StudioHeaderActions: tap-to-rename title (TextField inline), Publish (green key) / Unpublish (ghost) / Share (system share sheet with the URL). Expose \`func publishToggle()\` etc. through callbacks the integrator wires to JamAPI + a save-first.
- Bounce: BounceSheet (WAV / AAC keys) + Exporter: WAV writer from [Int16] planar PCM (16-bit, header exactly like ${HILMA}/../vibeceo/jambot/core/wav.js) and AAC .m4a via AVAssetWriter (44.1 kHz stereo, 192 kbps) from the same PCM, written to a temp file named like the web (jambot-<bpm>bpm-<date>.wav), then ShareSheet (UIActivityViewController; on Catalyst it becomes the share popover).
- About: build number (CFBundleShortVersionString + CFBundleVersion), engine bundle stamp (ask the integrator to pass \`EngineHost.version\` or read jambot-web.meta.json from the bundle if present), signed-in user, backend URL, Sign out.
Verify: build; simulator screenshots of Library with menu open, About, Catalog, PublicPlayer (public track "Exp1" by bart exists), BounceSheet; run Exporter in a debug launch arg (-exportSmoke) that writes both files from a synthetic 2 s sine and prints their sizes + \`afinfo\` output via os_log (or verify the files with afinfo from the shell using \`xcrun simctl get_app_container\`). Never publish/unpublish/delete anything under the bart account; use jamtest tracks (create/delete throwaways).
PROGRESS.md box 7. Report API + integration requests.` },
  { key: 'audio', model: undefined, effort: 'high', prompt: `${SHARED}
STAGE 8 — AUDIO (you). Files you own: ${APP}/Jambot/Engine/AudioPlayer.swift, NEW Services/RenderCache.swift, Services/NowPlaying.swift.
- RenderCache: async API \`static func key(sessionJSON: Data, stamp: String) -> String\` (SHA-256 via CryptoKit), \`func load(trackId:key:) async -> RenderResult?\`, \`func save(trackId:key:_ result: RenderResult) async\`, \`func drop(trackId:)\`; files in Caches/renders/<trackId>.pcm (planar Int16 as-is) + <trackId>.json sidecar (key, bpm, bars, hasArrangement, message, sampleRate, channels, length, savedAt); prune to the 6 most recent; never throw (log). Reading must be fast: a 128-bar stereo render is ~42 MB — use Data(contentsOf:) + withUnsafeBytes into [Int16] without per-sample copies.
- NowPlaying: MPNowPlayingInfoCenter (title = track title, artist "Jambot", album "jambot.to", duration = loop length, elapsed from player.position, playback rate) updated on play/stop/swap and every ~5 s while playing; MPRemoteCommandCenter play/pause/togglePlayPause/stop wired to closures the StudioModel provides; artwork = a generated 512×512 putty square with the wordmark (draw with UIGraphicsImageRenderer; keep it simple).
- AudioPlayer: handle AVAudioSession interruptions (pause on began; resume on ended if .shouldResume), route changes (headphones unplugged → pause), media services reset (rebuild the engine), background: keep the session active while playing; expose \`onStateChange\` already there; add \`isPlaying\`/\`position()\` guarantees after swap; make the hot swap sample-accurate at the phase (measure: position before/after swap within 10 ms) and document.
Verify: extend Engine/EngineSmoke.swift? NOT yours — instead add a -audioSmoke launch arg handler in a NEW file Services/AudioSmoke.swift that the integrator can route (say so), and until then verify with a unit-style run: build a tiny harness that the existing -engineSmoke can call? No — keep it simple: write AudioSmoke as a static func run(engine:) that logs PASS/FAIL for: cache round trip (save → load → identical [Int16] + metadata), swap phase delta < 10 ms, Now Playing info populated (read MPNowPlayingInfoCenter.default().nowPlayingInfo), interruption simulation via posting AVAudioSession.interruptionNotification with began/ended userInfo. The integrator routes -audioSmoke to it; you run it yourself by temporarily adding the route in JambotApp.swift, capturing the log, and REVERTING that edit (JambotApp.swift is not yours). Paste the log.
PROGRESS.md box 8. Report API + exact StudioModel integration steps (where to call cache load on open, save after whole-track renders with a 1.5 s debounce, NowPlaying hooks).` },
  { key: 'brand', model: 'sonnet', effort: 'medium', prompt: `${SHARED}
STAGE 9 — BRANDING + CATALYST (you). Files you own: ${APP}/Jambot/Assets.xcassets/** (AppIcon, LaunchBackground, AccentColor), NEW ${APP}/branding/ (icon source script + PNGs), Jambot/Theme.swift (additive), NEW Jambot/UI/CatalystSupport.swift, NEW Jambot/UI/KeyboardShortcuts.swift (a ViewModifier bundle the integrator applies), ${APP}/project.yml ONLY for Info.plist keys you need (UILaunchScreen, LSMinimumSystemVersion, UISupportsDocumentBrowser no; keep everything else), ${APP}/CLAUDE.md (document your bits).
- App icon: 1024×1024 putty enamel square (#e3e4dc with a very subtle top-left light gradient and a 1px inner hairline), "JAMBOT" in a condensed black uppercase face (use the system condensed/heavy weight, or draw with a bundled-free approach via Python Pillow if installed — check \`python3 -c "import PIL"\`; otherwise a Swift script with CoreGraphics run via \`swift\` from the shell) with a raised 909-orange LED disc right after the T (NEVER a period), plus the 16-step LED strip motif in three rows below the wordmark (orange/cobalt/ink cells, some lit). Generate all required sizes into AppIcon.appiconset (single 1024 universal entry is fine for iOS 17 / Catalyst) and render a preview PNG you look at. Keep the icon legible at 60px.
- Launch screen: UILaunchScreen with LaunchBackground colour = putty, no image (or the wordmark if trivial).
- CatalystSupport: on Mac, window min size 390×700 and a sensible default 430×860 (UIWindowScene.sizeRestrictions via a UIWindowSceneDelegate-free approach: SwiftUI \`.onAppear\` with UIApplication.shared.connectedScenes), title bar hidden or "Jambot", centred 720pt max column helper \`ColumnWidth\` modifier the integrator applies on regular width.
- KeyboardShortcuts: modifiers for space (play/stop), ⌘↩ (send), ⌘K (Controls), ⌘, (About) as \`.keyboardShortcut\` helpers + a doc block on where to apply.
- Theme.swift: add the panel palettes (JB202 teal, JT-30, JT-10, JT-90, JB01, FX) as named colours in case other agents want them; don't change existing tokens.
Verify: build iOS sim + Catalyst; \`xcrun simctl io "iPhone 16" screenshot\` of the home screen with the icon installed (use \`xcrun simctl launch\` then press home via \`xcrun simctl … \` is not possible — instead screenshot the app switcher? Not possible either: verify the icon by rendering AppIcon at 60/120/180 px into .shots/ and LOOKING at them); Catalyst: launch the built .app from the shell and screenshot the Mac window with \`screencapture -l <windowid>\` (get the id with \`osascript -e 'tell app "System Events" to id of window 1 of process "Jambot"'\` — that is not screen control, it is a read) — if it fails, say so.
PROGRESS.md box 9. Report files + integration requests.` },
]

phase('Build')
const built = await parallel(BUILDERS.map((b) => () =>
  agent(b.prompt, { label: `build:${b.key}`, phase: 'Build', ...(b.model ? { model: b.model } : {}), effort: b.effort })))
const reports = Object.fromEntries(BUILDERS.map((b, i) => [b.key, built[i] || `(${b.key} agent returned nothing — read PROGRESS.md and the files on disk)`]))
log(`builders done: ${BUILDERS.map((b, i) => `${b.key}=${built[i] ? 'ok' : 'FAILED'}`).join(' ')}`)

phase('Integrate')
const integ = await agent(`${SHARED}
STAGE 10 — INTEGRATE (you). Five builders finished (reports below). You own everything under ${APP} now. Wire it all: ControlsSheetView gets the Faders · Panels · Seq segmented header (remembered), Panels uses PanelsView with hits from engine.hits(step:scope:) polled per 16th from StudioModel's playhead clock (only when the sheet is open), Seq uses SeqView with onEdited → StudioModel (re-render 300 ms, autosave, coalesced notes) and onScope → renderScope (section audition: render(.section) with the same view trick, transport readout "section N · bar n/8", back to song on Done); StudioHeaderActions in the studio header (rename → save; publish/unpublish → save first then JamAPI; share sheet); BounceSheet from the transport Bounce key using the last RenderResult; RenderCache on open (key = serialize() JSON + engine version; play from cache, else render) and save 1.5 s after whole-track renders; NowPlaying + remote commands; AboutView from the Library; Catalog/PublicPlayer/Remix flows; route -panelsPreview/-seqPreview/-audioSmoke/-exportSmoke debug args; apply KeyboardShortcuts and CatalystSupport/ColumnWidth; icon + launch screen in place. Resolve protocol drift between builders (they built against DESIGN.md; small mismatches are yours to fix).
Verify in the iPhone 16 simulator with -autoLogin jamtest jamtest1 -openTrack "SEQ TEST techno copy" and an extended -studioScript: play; Faders fader; Panels open JB202 + knob drag + LED flashing (sample hits over 1 s, log); Seq: tap kick step 2 → pad lit + saved pattern changed (verify via serialize), section 2 edit isolation, loop-section audition transport text, Done returns to song; M/S; rename; Bounce WAV + AAC files exist (afinfo); About shows the build number; Library menu duplicate + delete of the duplicate; Catalog list + PublicPlayer plays "Exp1" + Remix into jamtest then delete the remix; render cache: second open of "SEQ TEST song 128" is fast (log timings) — screenshots of every state under .shots/, and LOOK at them. Catalyst build + launch (screenshot via screencapture -l if possible). Then ./bump-build.sh, device build + install per CLAUDE.md (phone may be locked → just report). Update PROGRESS.md (box 10, with evidence), CLAUDE.md (new launch args, features), DESIGN.md if anything deviated. Commit everything under apps/jamnative + scripts/ios (message: "Jambot native: full app — Seq, Panels, sharing, bounce, render cache, lock-screen audio, branding") and push.
REPORTS:
${Object.entries(reports).map(([k, r]) => `--- ${k} ---\n${typeof r === 'string' ? r : JSON.stringify(r)}`).join('\n\n')}`,
  { label: 'integrate', phase: 'Integrate', effort: 'xhigh' })

phase('Review')
const review = await agent(`${SHARED}
STAGE 11 — DESIGN REVIEW (you). The app is integrated (report below). Take the web app's screenshots for reference (JAM_URL=https://jambot.to with pw-harness.mjs at 390×844: library, studio, Controls Faders/Panels/Seq, sheet in song mode) and the native app's screenshots of the same states (simulator, -studioScript). Compare side by side as a designer: spacing math, type sizes/weights, key shapes and shadows, LED colours, header rhythm, sheet header, pill sizes, grid pad sizes, knob faces, empty states, Catalyst window. Fix what is off (you own all files under ${APP}); re-shoot; iterate until you would sign off. Also check Dynamic Type at the largest default size does not break the transport, and that nothing overflows at iPhone SE width (375). Update PROGRESS.md box 11 with the before/after list, commit ("Jambot native: design pass") and push.
INTEGRATION REPORT:
${typeof integ === 'string' ? integ : JSON.stringify(integ)}`,
  { label: 'design-review', phase: 'Review', effort: 'high' })

phase('TestFlight')
const tf = await agent(`${SHARED}
STAGE 12 — TESTFLIGHT (you). Follow ${HILMA}/apps/feynd/CLAUDE.md (TestFlight + "TestFlight for Mac" sections) and ${HILMA}/apps/feynd/testflight/ (export plists, asc-submit.mjs) as the working precedent; the ASC API key/issuer are in ${HILMA}/scripts/ios/asc-dev-profile.py. Steps: (1) generalise scripts/ios/asc-dev-profile.py (or add scripts/ios/asc-profile.py) to mint an IOS_APP_STORE profile "Jambot appstore" for com.bartdecrem.Jambot against the distribution certificate in this Mac's keychain ("Apple Distribution: Bart Decrem (274T5WCVD2)" — match by serial like the dev flow); (2) create the App Store Connect app record for com.bartdecrem.Jambot if missing (POST /v1/apps is not available — apps are created via the ASC website; check GET /v1/apps?filter[bundleId]; if missing, STOP that path and report exactly what Bart must click, then still do the archive/export so the .ipa is ready); (3) ./bump-build.sh, \`xcodebuild archive\` (generic/platform=iOS, manual signing with the appstore profile, Release), \`-exportArchive\` with an export plist copied from apps/feynd/testflight/export.plist (adjust bundle id/profile), upload with \`xcrun altool --upload-app -f <ipa> -t ios --apiKey 5A5HNSWA33 --apiIssuer <issuer>\`; (4) adapt asc-submit.mjs into apps/jamnative/testflight/asc-submit.mjs and run it once the build is VALID; (5) Catalyst upload only if the iOS one went cleanly and the Mac provisioning can be minted the same way (see Feynd's notes; otherwise report). Record every command and result in PROGRESS.md box 12 and apps/jamnative/CLAUDE.md (TestFlight section); commit ("Jambot native: TestFlight") and push. Be precise about what is blocked on Bart (the app record, agreements) — do not guess.
PREVIOUS REPORTS:
integration: ${typeof integ === 'string' ? integ.slice(0, 4000) : ''}
review: ${typeof review === 'string' ? review.slice(0, 3000) : ''}`,
  { label: 'testflight', phase: 'TestFlight', effort: 'high' })

return { builders: reports, integ, review, tf }