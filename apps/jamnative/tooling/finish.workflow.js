export const meta = {
  name: 'jambot-finish',
  description: 'Finish Jambot: native dark/light mode + full cosmetic review (Fable), jambot.to landing page + logo assets (Sonnet), then ship: archives, TestFlight prep, device install, docs, commits',
  phases: [
    { title: 'Build', detail: 'native theme+review (Fable) ∥ web landing+logos (Sonnet)' },
    { title: 'Ship', detail: 'icon into app, builds, archives, TestFlight, device, docs, commit+push' },
  ],
}

const HILMA = '/Users/bartdecrem/Documents/coding2025/hilma'
const APP = `${HILMA}/apps/jamnative`
const LOGOS = `${HILMA}/misc/jambotlogos`

const COMMON = `
RULES (all stages)
- NO SCREEN CONTROL: never call computer-use, claude-in-chrome, request_access or anything that drives the Mac's screen. Simulator only via xcrun simctl + the app's DEBUG launch args (-autoLogin jamtest jamtest1, -openTrack "<title>", -openControls, -studioScript "<steps>" with the shot:<name> handshake serviced by ${APP}/tooling/sim-run.sh — read it and ${APP}/Jambot/UI/StudioScript.swift + LibraryScript.swift for the vocabulary). Web via Playwright: ${HILMA}/scripts/jam/pw-harness.mjs (local playwright package; jamtest account). LOOK at every screenshot you take (Read the png) before calling a screen done.
- Backend is production (jambot.to). Only the jamtest / jamtest1 account. Never bart.
- Native: \`cd ${APP} && xcodegen generate && xcodebuild -project Jambot.xcodeproj -scheme Jambot -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -3\` must say BUILD SUCCEEDED; Catalyst: -destination 'platform=macOS,variant=Mac Catalyst'. Never open the Xcode GUI. ENABLE_DEBUG_DYLIB stays NO. No third-party packages. Never name a bundle subfolder "Resources".
- Web: after edits, \`pnpm exec tsc --noEmit\` clean; before the final commit \`pnpm build\` must pass (kill any \`next dev\` first; \`rm -rf .next\`). Playwright against a dev server you start on port 3100 (\`GOOD=$(grep -m1 '^SUPABASE_SERVICE_KEY=' .env.local | cut -d= -f2-); env PORT=3100 SUPABASE_SERVICE_KEY="$GOOD" pnpm dev\` with nohup, kill it when done).
- The logo assets are in ${LOGOS} (jambot-monogram-{light,dark}.png 1024², jambot-grid-{light,dark}.png, jambot-icons.zip with SVGs + transparent variants, preview.png). DECISION (already made, don't relitigate): the MONOGRAM (the "J" with the orange dot) is the app icon and the site mark; the grid stays a UI motif only.
- Commits: only the Ship stage commits. Builders update ${APP}/PROGRESS.md (native) with a dated log line under a new "## Finish (2026-09-06)" section, and report concisely.
`

const NATIVE = `${COMMON}
STAGE A — NATIVE: DARK/LIGHT MODE + FULL COSMETIC REVIEW (you; you own every file under ${APP} except PROGRESS.md sections written by others). Read ${APP}/DESIGN.md ("Full app" + "As built"), CLAUDE.md, Theme.swift, UI/Keys.swift, then every screen.
CONTEXT: Bart looked at build 0.1 (4) on his iPhone (which runs iOS DARK mode) and found cosmetic errors. His screenshot of the Controls sheet shows: the Done key rendered peach with a dark grey ring (not the solid orange rubber key with the 2pt lip), the "CONTROLS" title rendered WHITE on the putty header, the length key "128" wrapping onto two lines, and the fader tracks invisible to the right of the cap. Root causes to look for: system colours leaking (.primary/.secondary, default Button tint, Color(.systemBackground), .foregroundStyle defaults, List/Form backgrounds, TextField chrome, tint) which invert under iOS dark mode while the JBTheme tokens are fixed light hexes; the Done key's material/overlay; the adaptive grid min 40 for "128". He then asked for proper Dark and Light modes.
1. APPEARANCE: an Appearance setting (System / Light / Dark) stored in UserDefaults ("jam.appearance"), shown in AboutView as a 3-segment control, applied at the root with .preferredColorScheme (nil for System). Make EVERY JBTheme colour token dynamic: Color(uiColor: UIColor { trait in trait.userInterfaceStyle == .dark ? dark : light }) so all existing call sites adapt. Dark palette ("the desk instrument at night", keep the same material logic: enamel panel, raised cards, recessed wells, paper inputs): panel 0x1B1D20, panel2 0x24272B, panel3 0x141618, panel4 0x2C3035, ink 0xECEEE8, ink2 0xB9BCB4, ink3 0x8A8E86, rule 0x3A3E43, ledOff 0x3A3E43, orange 0xFF5A2A, orangeSoft 0x8A3A22, cobalt 0x5A80FF, green 0x1DB981. KEYS: in dark mode the "ink rubber key" must stay a dark-on-light key or it vanishes — define semantic tokens keyFill / keyLabel / keyLip (light: ink / panel2 / #000; dark: 0xE6E8E1 / 0x14161A / 0x0A0B0C) and use them in JBKeyStyle .ink/.default and wherever a solid ink key is drawn (transport Play, CONTROLS key, segmented "on" pill, section pill .on, Send). Panel/ghost keys use panel4/panel3 with rule strokes. LED strip: lit cells keep orange/cobalt; the "kick" row's ink cells become putty in dark. The synth panels (Panels tab) already have their own dark skins — leave those palettes alone in both modes but make sure their headers/M-S keys still read. Chat bubbles: user bubble = keyFill/keyLabel; assistant text = ink. Status bar style follows the scheme.
2. REVIEW EVERY SCREEN in BOTH modes on the iPhone 16 simulator (\`xcrun simctl ui "iPhone 16" appearance dark|light\`) and at iPhone SE width if a device is available (create one with simctl if not: iPhone SE (3rd generation) device type may not exist on iOS 27 — then use the narrowest available). Screens: Login (with catalog), Library (menu open, tags), Studio (empty state; chat with tool chips; playing), Controls Faders (Track card incl. the length keys — "128" on one line: min key width 44 or a flexible HStack — and fader tracks visible: 5pt track in a colour that contrasts with the card in both modes), Panels (collapsed; JT-90, JB202, JT-10 open; effect), Seq (drums, mono editor, section pills, armed CLEAR), Bounce sheet, Publish/Share keys, About (with the new Appearance control), Public player, Catalog. Check: no system-colour leaks (nothing white-on-putty or black-on-charcoal), key lips/press states, alignment to the 16pt margin, type sizes vs the web (${HILMA}/src/app/jam/jam.css and the web screenshots in ${APP}/.shots/web/ if present — else take them with pw-harness against JAM_URL=https://jambot.to), Dynamic Type at the largest default size not breaking the transport, safe areas on the sheet, Catalyst build still green. Fix everything you find; re-shoot; iterate until a designer would sign off in both modes. Save the final shots as ${APP}/.shots/final-light/*.png and .shots/final-dark/*.png (one per screen, named).
3. APP ICON: replace AppIcon.appiconset with the monogram: light appearance = ${LOGOS}/jambot-monogram-light.png, dark appearance = jambot-monogram-dark.png, tinted = a grayscale version of the transparent dark monogram from jambot-icons.zip (white J + dot on transparent → grayscale, alpha kept). Use the iOS 18+ appiconset Contents.json with "appearances" entries (luminosity dark / tinted) on the single 1024 universal image; also the ios-marketing entry. Launch screen background stays putty in light and panel-dark in dark (LaunchBackground colour set with both appearances). Render the icon at 60/120/180 px into .shots/ and look at it.
4. Verify builds (iOS sim + Catalyst) green; run one full -studioScript pass in dark mode to prove nothing functional broke (play, fader, Seq tap, Panels open, M/S, About appearance switch persists across relaunch). PROGRESS.md: add "## Finish (2026-09-06)" with a dated line: what changed, palette, the list of cosmetic fixes (each: symptom → cause → fix), screenshot folders. Do not commit.
Return: concise report (fixes list, palette, files, screenshot folders, anything not done).`

const WEB = `${COMMON}
STAGE B — WEB: jambot.to LANDING PAGE + LOGO ASSETS (you; you own ${HILMA}/src/app/jam/** except nothing under alt/, seq/, Studio.tsx, ControlsSheet.tsx — you may edit JamApp.tsx, AuthScreen.tsx, Catalog.tsx, layout.tsx, page.tsx, jam.css (append only), opengraph-image.tsx, plus ${HILMA}/public/jam/** for assets, and one paragraph in ${HILMA}/CLAUDE.md's Jam section). Read ${HILMA}/CLAUDE.md "Jam" section, src/app/jam/JamApp.tsx, AuthScreen.tsx, Catalog.tsx, layout.tsx, jam.css, opengraph-image.tsx first.
1. LANDING MODE: a first-time (signed-out) visitor to jambot.to / /jam sees a landing, not a bare login form. Structure (single column, 16px margins, max-width 720 on wide screens like the app): (a) hero — the monogram mark (the DARK monogram tile: ink tile, putty J, orange dot, from ${LOGOS}/jambot-monogram-dark.png, exported to public/jam/mark-dark.png at 256² plus an SVG copy from jambot-icons.zip if it renders identically) at 72px with 22% corner radius, the JAMBOT wordmark (existing .jb-wordmark with the raised LED, never a period), one line of positioning in the panel face: "A groovebox you talk to." and ONE sentence in the body face drawn from the Jambot README (${HILMA}/../vibeceo/jambot/dist/README.md): e.g. "Say “techno at 128 with a 909 kick and offbeat hats” and real synth engines — 909 drums, 303 acid, 101 leads — play it back in seconds. Every parameter is tweakable, every pattern is yours." (trim to one sentence; keep it plain, no hype words, no emoji, no exclamation marks); (b) two keys: "Start a track" (orange, → the create-account form) and "Sign in" (panel); (c) "LISTEN" eyebrow + the public catalog (existing Catalog component) so a visitor can play tracks without an account; (d) the sign-in / create-account form below or revealed by the keys (a small in-page toggle is fine; keep the existing AuthScreen logic and the ?remix= hint working); (e) footer line "Made by Bart Decrem · jambot.to" in jb-readout. Returning visitors (localStorage jam:seen or an existing session cookie → /api/jam/auth/me 200) go straight to the library as today. Landing is light-only (the web app is light); mobile-first: check 390×844 and 1280×800.
2. ASSETS: favicon.ico + icon.png (public/jam/icon-192.png, icon-512.png) + apple-touch-icon (180) from the LIGHT monogram (putty tile) for the site; wire them in src/app/jam/layout.tsx metadata (icons: { icon, apple }) — note the site is served under /jam and via the jambot.to host rewrite, so use absolute /jam/… paths. Update src/app/jam/opengraph-image.tsx to include the monogram mark next to the wordmark (Satori: use an <img> with an absolute URL or inline base64 PNG; keep display:flex on any element with two children).
3. Verify: tsc clean; Playwright screenshots (fresh context = landing) at 390×844 and 1280×800 → ${HILMA}/misc/jambotlogos/../../.claude-shots is NOT allowed — put them in ${APP}/.shots/web-landing/ (gitignored); check the OG image route renders (GET /jam/opengraph-image returns 200 and looks right — screenshot it); sign in as jamtest still lands in the library; ?remix=<slug> flow still shows the hint; a public track plays from the landing. LOOK at the screenshots as a designer (hero spacing math in a comment, keys aligned to the margin, wordmark LED raised, nothing centred-by-accident). Then kill the dev server, \`rm -rf .next && pnpm build\` must pass. Add one paragraph to ${HILMA}/CLAUDE.md's Jam section (landing mode, assets, where the logo sources live). Do not commit.
Return: concise report (what the landing says, files, screenshot paths, build result).`

phase('Build')
const [native, web] = await parallel([
  () => agent(NATIVE, { label: 'native:theme+review', phase: 'Build', effort: 'xhigh' }),
  () => agent(WEB, { label: 'web:landing+logos', phase: 'Build', model: 'sonnet', effort: 'high' }),
])
log(`build: native ${native ? 'ok' : 'FAILED'}, web ${web ? 'ok' : 'FAILED'}`)

phase('Ship')
const ship = await agent(`${COMMON}
STAGE C — SHIP (you; you own everything). Reports below. Steps, in order, recording each result:
1. Sanity: \`git status\`; both native builds green (iOS sim + Catalyst); \`cd ${HILMA} && pnpm exec tsc --noEmit\` clean; if the web agent did not run \`pnpm build\`, run it (kill any next dev, rm -rf .next). Fix small breakage yourself.
2. Commit + push in two commits: (a) web: "Jambot: landing page for first-time visitors, monogram mark + icons" (src/app/jam, public/jam, CLAUDE.md); (b) native: "Jambot native: dark/light appearance, cosmetic pass, monogram app icon" (apps/jamnative). Trailers on every commit:
   Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
   Claude-Session: https://claude.ai/code/session_017W56qk3MbjaLQ8uRtrVo4V
3. Device: \`cd ${APP} && ./bump-build.sh\`, then the device build/install/launch commands in ${APP}/CLAUDE.md "Device install" (profile "Jambot dev" is installed; use -derivedDataPath build-device). If launch is refused because the phone is locked, say so — the install still counts.
4. TestFlight, following ${HILMA}/apps/feynd/CLAUDE.md ("TestFlight upload" + "TestFlight for Mac") and ${APP}/testflight/*.plist (already adapted; export.plist has destination "export"): \`export DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer\` (release Xcode, not the beta); check the appstore profile is installed (security cms -D on ~/Library/Developer/Xcode/UserData/Provisioning Profiles/*.mobileprovision | grep "Jambot appstore"; if missing: \`python3 ${HILMA}/scripts/ios/asc-profile.py com.bartdecrem.Jambot "Jambot appstore" appstore\`); archive iOS Release (manual signing, "Jambot appstore", Apple Distribution) into ${APP}/build-testflight/Jambot.xcarchive; -exportArchive with testflight/export.plist to build-testflight/ipa/ (produces Jambot.ipa). Then check for the App Store Connect app record: GET /v1/apps?filter[bundleId]=com.bartdecrem.Jambot with the team key (see scripts/ios/asc-profile.py for JWT). If a record exists: \`xcrun altool --upload-app -f build-testflight/ipa/Jambot.ipa -t ios --apiKey 5A5HNSWA33 --apiIssuer 69a6de80-eb13-47e3-e053-5b8c7c11a4d1\`, then create ${APP}/testflight/asc-submit.mjs from apps/feynd/testflight/asc-submit.mjs with the Jambot app id and an added step that creates (or reuses) a beta group named "Public" with publicLinkEnabled true and publicLinkLimit 1000 (POST /v1/betaGroups with the app relationship, then PATCH publicLinkEnabled) and prints the public link, and run it. If NO record exists: try POST /v1/apps once (expect 403/405 — Apple does not allow creating apps via the API; record the exact response), then STOP that path and write the exact clicks Bart must do at appstoreconnect.apple.com → My Apps → + → New App (Platforms iOS + macOS, Name "Jambot", Primary language English (U.S.), Bundle ID com.bartdecrem.Jambot (it is registered), SKU jambot, User access Full) and the single command that then uploads the already-exported IPA. Catalyst archive: attempt it too (profile "Jambot catalyst appstore" via asc-profile.py kind catalyst-appstore; export-mac.plist; the installer cert WUZK4CR87J is in the keychain) so the .pkg is ready; if anything in the Mac path fails, note it and move on — iOS is the priority.
5. Docs: ${APP}/CLAUDE.md gets a "TestFlight" section with the exact commands + standing facts (profile ids, app record status); ${APP}/PROGRESS.md boxes 12 + a final status block (what is live, what waits on Bart); update the "Jam" paragraph in ${HILMA}/CLAUDE.md only if the web agent didn't. Commit "Jambot native: TestFlight archive + docs" and push.
Return: build numbers, commit hashes, device install result, TestFlight status (uploaded + public link, or exactly what Bart must click), Catalyst status, and anything left.
NATIVE REPORT:
${typeof native === 'string' ? native : JSON.stringify(native) || '(native agent returned nothing — read PROGRESS.md and git status)'}
WEB REPORT:
${typeof web === 'string' ? web : JSON.stringify(web) || '(web agent returned nothing — read git status)'}`,
  { label: 'ship', phase: 'Ship', effort: 'high' })

return { native, web, ship }