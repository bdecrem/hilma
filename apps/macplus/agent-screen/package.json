{
  "name": "macinclaude-screen",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Screen agent: on-demand screenshot of the Macintosh Plus over WiFi (mux channel 'screen'). Sends GRAB to the Plus, receives the RLE'd 1-bit framebuffer, writes a PNG.",
  "bin": { "macinclaude-screen": "src/main.ts" },
  "scripts": { "start": "tsx src/main.ts --listen 2334" },
  "devDependencies": { "@types/node": "latest", "tsx": "latest", "typescript": "latest" }
}
