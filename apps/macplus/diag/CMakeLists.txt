cmake_minimum_required(VERSION 3.5)
project(DiagTest C)
add_application(DiagTest diagtest.c)
set_target_properties(DiagTest PROPERTIES COMPILE_OPTIONS -ffunction-sections)
if(CMAKE_SYSTEM_NAME MATCHES Retro68)
    set_target_properties(DiagTest PROPERTIES LINK_FLAGS "-Wl,-gc-sections -Wl,--mac-strip-macsbug")
else()
    set_target_properties(DiagTest PROPERTIES LINK_FLAGS "-Wl,-gc-sections")
endif()
