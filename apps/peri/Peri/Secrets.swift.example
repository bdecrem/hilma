import Foundation

// Copy to Secrets.swift (gitignored) and point at your backend.
// Production: https://feynd.cc — Peri shares accounts/topics/cards with
// F2 and Loci. Local dev: http://127.0.0.1:3000 (simulator only).
enum Secrets {
    static let backendBaseURL = URL(string: "https://feynd.cc")!
}
