import Foundation

// Copy to Secrets.swift (gitignored) and point at your backend.
// Production: https://feynd.cc — Loci shares accounts and topics with F2.
// Local dev: http://127.0.0.1:3000 (simulator only; needs the dev server up).
enum Secrets {
    static let backendBaseURL = URL(string: "https://feynd.cc")!
}
