import Foundation

// Copy to Secrets.swift (gitignored). appKey = SURF_APP_KEY from hilma's .env.local / Vercel.
enum Secrets {
    static let backendURL = URL(string: "https://hilma-nine.vercel.app")!
    static let appKey = "SURF_APP_KEY goes here"
}
